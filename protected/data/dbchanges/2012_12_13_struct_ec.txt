ALTER TABLE `actions_in` ADD `AIN_DEFAULT` VARCHAR( 100 ) NOT NULL AFTER `AIN_ID` ,
ADD `AIN_PREP` ENUM( 'Y', 'N' ) NOT NULL DEFAULT 'N' AFTER `AIN_DEFAULT`;
ALTER TABLE `steps` ADD `STE_PREP` ENUM( 'Y', 'N' ) NOT NULL DEFAULT 'N' AFTER `STE_STEP_NO` ;