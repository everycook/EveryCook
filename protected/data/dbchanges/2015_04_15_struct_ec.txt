ALTER TABLE `recipes` CHANGE `REC_SERVING_COUNT` `REC_SERVING_COUNT` INT(11) NOT NULL;
ALTER TABLE `recipes` ADD `REC_RATING` DECIMAL NULL AFTER `REC_COMPLEXITY`, 
ADD `REC_TIME_PREP` INT NOT NULL DEFAULT '0' AFTER `REC_RATING`, 
ADD `REC_TIME_COOK` INT NOT NULL DEFAULT '0' AFTER `REC_TIME_PREP`,
ADD `REC_TIME_TOTAL` INT NOT NULL DEFAULT '0' AFTER `REC_TIME_COOK`;

ALTER TABLE `recipes_history` CHANGE `REC_SERVING_COUNT` `REC_SERVING_COUNT` INT(11) NOT NULL;
ALTER TABLE `recipes_history` ADD `REC_RATING` DECIMAL NULL AFTER `REC_COMPLEXITY`, 
ADD `REC_TIME_PREP` INT NOT NULL DEFAULT '0' AFTER `REC_RATING`, 
ADD `REC_TIME_COOK` INT NOT NULL DEFAULT '0' AFTER `REC_TIME_PREP`,
ADD `REC_TIME_TOTAL` INT NOT NULL DEFAULT '0' AFTER `REC_TIME_COOK`;


CREATE TABLE IF NOT EXISTS `difficulty` (
  `DIF_ID` int(11) NOT NULL,
  `DIF_ORDER` int(11) NOT NULL,
  `DIF_DESC_EN_GB` varchar(100) COLLATE utf8_unicode_ci NOT NULL,
  `DIF_DESC_DE_CH` varchar(100) COLLATE utf8_unicode_ci NOT NULL,
  `CREATED_BY` int(11) NOT NULL,
  `CREATED_ON` int(11) NOT NULL,
  `CHANGED_BY` int(11) NOT NULL,
  `CHANGED_ON` int(11) NOT NULL
) ENGINE=MyISAM DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;
ALTER TABLE `difficulty` ADD PRIMARY KEY( `DIF_ID` );
ALTER TABLE `difficulty` CHANGE `DIF_ID` `DIF_ID` INT(11) NOT NULL AUTO_INCREMENT;

ALTER TABLE `recipes` CHANGE `REC_COMPLEXITY` `DIF_ID` INT(11) NULL DEFAULT NULL;
ALTER TABLE `recipes_history` CHANGE `REC_COMPLEXITY` `DIF_ID` INT(11) NULL DEFAULT NULL;
