ALTER TABLE `recipes` ADD FULLTEXT `TEXT_EN_GB` (
`REC_NAME_EN_GB` ,
`REC_SYNONYM_EN_GB`
);

ALTER TABLE `recipes` ADD FULLTEXT `TEXT_DE_CH` (
`REC_NAME_DE_CH` ,
`REC_SYNONYM_DE_CH`
);

CREATE TABLE `recipe_index_data` (
`RID_LANG` VARCHAR( 5 ) NOT NULL ,
`RID_TEXT` VARCHAR( 100 ) NOT NULL ,
`RID_COUNT` INT NOT NULL
) ENGINE = MYISAM ;

ALTER TABLE `recipe_index_data` ADD PRIMARY KEY ( `RID_LANG` , `RID_TEXT` );
