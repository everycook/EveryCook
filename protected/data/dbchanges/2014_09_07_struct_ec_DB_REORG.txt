CREATE TABLE `cusine_types` (
`CUT_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`CUT_GPS_LAT` DOUBLE NOT NULL ,
`CUT_GPS_LNG` DOUBLE NOT NULL ,
`CUT_GOOGLE_REGION` VARCHAR( 50 ) NOT NULL ,
`CUT_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`CUT_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;

CREATE TABLE `cusine_sub_types` (
`CST_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`CUT_ID` INT NOT NULL ,
`CST_GPS_LAT` DOUBLE NOT NULL ,
`CST_GPS_LNG` DOUBLE NOT NULL ,
`CST_GOOGLE_REGION` VARCHAR( 50 ) NOT NULL ,
`CST_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`CST_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;

CREATE TABLE `relation_typs` (
`REL_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`REL_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`REL_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `recipe_relations` (
`REC_ID1` INT NOT NULL,
`REC_ID2` INT NOT NULL,
`REL_ID` INT NOT NULL,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `tags` (
`TAG_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`TAG_IGNORE` ENUM( 'N', 'Y' ) NOT NULL,
`TAG_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`TAG_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `rec_to_tag` (
`REC_ID` INT NOT NULL,
`TAG_ID` INT NOT NULL,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;



CREATE TABLE `food_allergy` (
`FAL_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`FAL_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`FAL_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `ing_to_fal` (
`ING_ID` INT NOT NULL,
`FAL_ID` INT NOT NULL,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `diet` (
`DIE_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`DIE_VALUES` VARCHAR( 200 ) NOT NULL ,
`DIE_DESC_EN_GB` VARCHAR( 100 ) NOT NULL ,
`DIE_DESC_DE_CH` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;


CREATE TABLE `cooking_stove` (
`COS_ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`COS_MANUFACTURE` VARCHAR( 100 ) NOT NULL ,
`COS_MODEL` VARCHAR( 100 ) NOT NULL ,
`COS_LEVELS` int NOT NULL ,
`COS_CONTINIOUS` ENUM( 'N', 'Y' ) NOT NULL,
`COS_MAESUREMENT` VARCHAR( 100 ) NOT NULL ,
`CREATED_BY` INT NOT NULL ,
`CREATED_ON` INT NOT NULL ,
`CHANGED_BY` INT NOT NULL ,
`CHANGED_ON` INT NOT NULL
) ENGINE = MYISAM;




ALTER TABLE `ingredients` ADD `ING_HAS_ALLERGY_INFO` ENUM( 'N', 'Y' ) NOT NULL DEFAULT 'N' AFTER `ING_IMG_ETAG` ,
ADD `ING_NEED_PEELING` ENUM( 'N', 'Y', 'O' ) NOT NULL DEFAULT 'N' AFTER `ING_HAS_ALLERGY_INFO` ,
ADD `ING_NEED_WASH` ENUM( 'N', 'Y' ) NOT NULL DEFAULT 'N' AFTER `ING_NEED_PEELING` ,
ADD `ING_SCALE_PRECISION` INT( 3 ) NOT NULL DEFAULT '0' AFTER `ING_NEED_WASH` ,
ADD `ING_APPROVED` ENUM( '0', 'N', 'Y' ) NOT NULL DEFAULT '0' AFTER `ING_SCALE_PRECISION` ,
ADD `ING_WIKI_LINK` VARCHAR( 200 ) NULL AFTER `ING_APPROVED` ,
ADD `ING_WEIGHT_SMALL` INT NULL AFTER `ING_WIKI_LINK` ,
ADD `ING_WEIGHT_BIG` INT NULL AFTER `ING_WEIGHT_SMALL` ,
ADD `ING_SYNONYM_EN_GB` VARCHAR( 200 ) NULL AFTER `ING_WEIGHT_BIG` ,
ADD `ING_SYNONYM_DE_CH` VARCHAR( 200 ) NULL AFTER `ING_SYNONYM_EN_GB`;


ALTER TABLE `recipes` ADD `REC_HAS_ALLERGY_INFO` ENUM( 'N', 'Y' ) NOT NULL DEFAULT 'N' AFTER `REC_KCAL` ,
ADD `REC_SUMMARY` VARCHAR( 200 ) NULL AFTER `REC_HAS_ALLERGY_INFO` ,
ADD `REC_APPROVED` ENUM( '0', 'N', 'Y' ) NOT NULL DEFAULT '0' AFTER `REC_SUMMARY` ,
ADD `REC_SERVING_COUNT` INT NULL DEFAULT NULL AFTER `REC_APPROVED` ,
ADD `REC_WIKI_LINK` VARCHAR( 200 ) NULL DEFAULT NULL AFTER `REC_SERVING_COUNT` ,
ADD `REC_IS_PRIVATE` ENUM( 'N', 'Y' ) NOT NULL DEFAULT 'N' AFTER `REC_WIKI_LINK` ,
ADD `REC_COMPLEXITY` INT NULL DEFAULT NULL AFTER  `REC_IS_PRIVATE` ,
ADD `CUT_ID` INT NULL DEFAULT NULL AFTER `REC_COMPLEXITY` ,
ADD `CST_ID` INT NULL DEFAULT NULL AFTER `CUT_ID` ,
ADD `REC_CUSINE_GPS_LAT` DOUBLE NULL DEFAULT NULL AFTER `CST_ID` ,
ADD `REC_CUSINE_GPS_LNG` DOUBLE NULL DEFAULT NULL AFTER `REC_CUSINE_GPS_LAT` ,
ADD `REC_TOOLS` VARCHAR( 100 ) NULL AFTER `REC_CUSINE_GPS_LNG` ,
ADD `REC_SYNONYM_EN_GB` VARCHAR( 200 ) NULL AFTER `REC_TOOLS` ,
ADD `REC_SYNONYM_DE_CH` VARCHAR( 200 ) NULL AFTER `REC_SYNONYM_EN_GB`;





